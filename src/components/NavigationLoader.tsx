"use client";

// LOADER DISABLED - Navigation loader removed as it was showing on every page change
// If you need a loader, implement it per-page using Next.js loading.tsx files

export default function NavigationLoader() {
  // Loader disabled - no more annoying loaders on every navigation!
  return null;
}
